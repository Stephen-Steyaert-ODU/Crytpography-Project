services:
  crypto:
    build:
      context: .
      target: runtime
    volumes:
      - ./data:/data
    working_dir: /data
    entrypoint: ["/app/cryptography"]

  test:
    build:
      context: .
      target: test
